Drop your real prediction_market.json IDL file in this folder once you've
deployed the program (export it from Solana Playground, or copy it from
target/idl/prediction_market.json after running `anchor build`).

Then in src/App.tsx, uncomment the real IDL import and set DEMO_MODE to
false in src/config.ts.
